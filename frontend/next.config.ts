/** @type {import('next').NextConfig} */
const nextConfig = {
  // สำหรับ Next.js เวอร์ชั่นใหม่ๆ (15+) บางทีต้องใส่ไว้ใน experimental
  experimental: {
    
  },
  allowedDevOrigins: ['192.168.3.20'],
};

module.exports = nextConfig;